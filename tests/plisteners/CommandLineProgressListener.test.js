const CommandLineProgressListener = require('../../index').plisteners.CommandLineProgressListener;
const should = require('chai').should();

describe('x', function () {
    it('x', function () {
        // x.should.equal('x');
    });
});

