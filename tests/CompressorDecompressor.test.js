const CompressorDecompressor = require('../index').CompressorDecompressor;
const should = require('chai').should();

describe('CompressorDecompressor should function properly', function () {
    it('x', function () {
        // x.should.equal('x');
    });
});

