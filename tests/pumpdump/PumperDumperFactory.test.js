const PumperDumperFactory = require('../../index').pumpdump.PumperDumperFactory;
const should = require('chai').should();

describe('x', function () {
    it('x', function () {
        // x.should.equal('x');
    });
});

