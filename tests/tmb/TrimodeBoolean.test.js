const TrimodeBoolean = require('../../index').tmb.TrimodeBoolean;
const should = require('chai').should();

describe('x', function () {
    it('x', function () {
        // x.should.equal('x');
    });
});

