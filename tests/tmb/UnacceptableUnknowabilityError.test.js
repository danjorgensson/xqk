const UnacceptableUnknowabilityError = require('../../index').tmb.UnacceptableUnknowabilityError;
const should = require('chai').should();

describe('x', function () {
    it('x', function () {
        // x.should.equal('x');
    });
});

