const SandersonPseudoPellet = require('../../../index').shell.pellets.SandersonPseudoPellet;
const should = require('chai').should();

describe('x', function () {
    it('x', function () {
        // x.should.equal('x');
    });
});

