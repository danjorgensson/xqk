const PseudoPellet = require('../../../index').shell.pellets.PseudoPellet;
const should = require('chai').should();

describe('x', function () {
    it('x', function () {
        // x.should.equal('x');
    });
});

