const PelletFactory = require('../../../index').shell.pellets.PelletFactory;
const should = require('chai').should();

describe('x', function () {
    it('x', function () {
        // x.should.equal('x');
    });
});

