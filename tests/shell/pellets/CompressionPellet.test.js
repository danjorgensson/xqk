const CompressionPellet = require('../../../index').shell.pellets.CompressionPellet;
const should = require('chai').should();

describe('x', function () {
    it('x', function () {
        // x.should.equal('x');
    });
});

