const ClossonTypeProvider = require('../../../index').shell.typeproviders.ClossonTypeProvider;
const should = require('chai').should();

describe('x', function () {
    it('x', function () {
        // x.should.equal('x');
    });
});

