const FullertonTypeProvider = require('../../../index').shell.typeproviders.FullertonTypeProvider;
const should = require('chai').should();

describe('x', function () {
    it('x', function () {
        // x.should.equal('x');
    });
});

